To use the demo repo, just `cd` into it and run the build scripts:

```sh
cd demo/a
pnpm build
```

To run buildc directly from the CLI in one of these folders, use the `pnpm buildc` script:

```sh
cd demo
pnpm buildc graph
pnpm buildc all
```
